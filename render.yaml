services:
  - type: web
    name: playwright-mcp
    runtime: docker
    plan: standard
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "8080"
    dockerCommand: node cli.js --headless --browser chromium --no-sandbox --port 8080
